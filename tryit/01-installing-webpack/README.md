In this exercise we create a new NPM project and then install Webpack as a development dependency.

Development dependencies are libraries are are useful for development but not required during runtime.
